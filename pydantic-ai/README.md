# Pydantic AI example

Minimal type-safe agent. Good for production, MCP, and evaluation.

## Setup

Copy `../.env.example` to `.env` and set `OPENAI_API_KEY`. Then:

```bash
uv sync
```

## Run

```bash
uv run python main.py
```

Docs: [Pydantic AI](https://ai.pydantic.dev/)

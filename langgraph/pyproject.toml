[project]
name = "langgraph-example"
version = "0.1.0"
description = "Minimal LangGraph agent"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=0.2",
    "langchain-openai>=0.2",
    "langchain-core>=0.3",
]
